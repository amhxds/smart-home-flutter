import 'package:flutter/material.dart';

class Room2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Room 2 Layout'),
      ),
    );
  }
}